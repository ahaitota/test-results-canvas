{
  "name": "test-results-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Copilot canvas extension that renders .NET TRX and JUnit XML test runs as a live UI panel.",
  "type": "module",
  "main": "dist/extension.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/build-client.mjs",
    "build:client": "node scripts/build-client.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "typecheck:client": "tsc -p src/client/tsconfig.json",
    "typecheck:e2e": "tsc -p e2e/tsconfig.json",
    "typecheck:sdk": "node --import tsx scripts/typecheck-sdk.ts",
    "test": "node --import tsx --test test/labels.test.ts test/trx.test.ts test/junit.test.ts test/validate.test.ts",
    "test:ci": "node -e \"require('node:fs').mkdirSync('test-results',{recursive:true})\" && node --import tsx --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=test-results/unit-junit.xml test/labels.test.ts test/trx.test.ts test/junit.test.ts test/validate.test.ts",
    "test:e2e": "playwright test",
    "pretest:e2e": "npm run build"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@github/copilot-sdk": "^1.0.8",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.1",
    "preact": "^10.29.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
